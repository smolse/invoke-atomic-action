name: Test a Windows Technique
on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: ./
        with:
          technique: T1055.003
          test-numbers: 1
